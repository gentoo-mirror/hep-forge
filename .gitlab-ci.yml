stages:
  - triggers

trigger-pipelines:
  parallel:
    matrix:
      - CATEGORY: ["sci-physics"]
        PKG: [hepmc]
  stage: triggers
  trigger:
    include: $CATEGORY/$PKG/.gitlab-ci.yml
  rules:
    - changes:
        - $CATEGORY/$PKG/*